

#include <string>
#include <iostream>
#include <iomanip>
using namespace std;

int main(int argc, char argv[]){

	int c, len, d, Ncode = 0;
	unsigned int note[7] = { 0, 0, 0, 0, 0, 0, 0 };
	char decoded, coded[2][500], abc[40];

	string  S[7] = { "и", "эта", "нова€", "весна", "нам", "не", "вернет" }, incstr; //она сказала, что нужно сделать так, чтобы стринг не зависел от 7,ну и вообще от какого-либо числа. я чет не знаю как это сделать(

	for (int i = 0; i < 7; i++)
	{
		c = S[i][0];
		len = S[i].length();
		for (int j = 0; j < len - 1; j++)
		{
			note[i] += c;
			c = S[i][j + 1];
		}
		note[i] += c;
		note[i] %= 256;

	}
	cout << "Vvedite frazu:   ";
	getline(cin, incstr);
	len = incstr.length();
	cout << "Zakodirovannaya fraza:";
	for (int i = 0; i < len; i++)
	{
		coded[0][i] = (incstr[i] + note[(i % 7) - 1]) % 256;
		cout << coded[0][i];
	}

	cout << endl << "Ishodnaya fraza:  ";
	for (int i = 0; i < len; i++)
	{
		coded[1][i] = coded[0][i] - note[(i % 7) - 1];
		cout << coded[1][i];
	}
	// здесь заканчиваетс€ шифрование, и теперь нужна статистика. ќна сказала делать ее с помощью матриц. ’елпанешь?
	system("pause");
	return 0;
}